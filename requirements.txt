# Pinned exact (2026-07-13): pyarrow 25.0.0 segfaulted libarrow inside the running
# console (kernel: "segfault ... in libarrow.so.2500", exit 139 on every dashboard
# render) — pins match the last known-good image. Bump deliberately, one at a time.
streamlit==1.58.0
pandas==3.0.3
numpy==2.4.6
pyarrow==24.0.0
psycopg2-binary>=2.9.0
requests>=2.31.0
openpyxl>=3.1.0
